---
title: "Obtenir Utilisateur"
api: "GET /api/v1/users/{id}"
description: "Obtenir un utilisateur par ID"
---

## Paramètres de Chemin

<ParamField path="id" type="string" required>
  L'identifiant unique de l'utilisateur à récupérer
</ParamField>

## Réponse

<ResponseField name="id" type="string">
  Identifiant unique de l'utilisateur
</ResponseField>

<ResponseField name="name" type="string">
  Nom complet de l'utilisateur
</ResponseField>

<ResponseField name="email" type="string">
  Adresse e-mail de l'utilisateur
</ResponseField>

<ResponseField name="created_at" type="string">
  Date de création du compte (format ISO 8601)
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET https://api.example.com/api/v1/users/user_123 \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```python Python
import requests

url = "https://api.example.com/api/v1/users/user_123"
headers = {"Authorization": "Bearer YOUR_API_KEY"}

response = requests.get(url, headers=headers)
print(response.json())
```

```javascript JavaScript
const response = await fetch('https://api.example.com/api/v1/users/user_123', {
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY'
  }
});

const data = await response.json();
console.log(data);
```
</RequestExample>

<ResponseExample>
```json 200 Succès
{
  "id": "user_123",
  "name": "Marie Dupont",
  "email": "marie@example.com",
  "bio": "Développeuse de logiciels",
  "created_at": "2024-01-15T10:30:00Z",
  "updated_at": "2024-01-20T14:25:00Z"
}
```

```json 404 Non Trouvé
{
  "error": {
    "code": "USER_NOT_FOUND",
    "message": "Utilisateur avec l'ID spécifié introuvable"
  }
}
```

```json 401 Non Autorisé
{
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Un token d'authentification valide est requis"
  }
}
```
</ResponseExample>
