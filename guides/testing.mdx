---
title: "Testing Strategies"
description: "Comprehensive guide to unit testing, integration testing, and E2E testing"
---

# Testing Strategies

A robust testing strategy ensures code quality and prevents regressions. This guide covers unit, integration, and end-to-end testing.

## Unit Testing

### Jest for JavaScript/TypeScript

```typescript
// math.ts
export function add(a: number, b: number): number {
  return a + b;
}

export function divide(a: number, b: number): number {
  if (b === 0) {
    throw new Error('Division by zero');
  }
  return a / b;
}

export class Calculator {
  private history: number[] = [];

  add(a: number, b: number): number {
    const result = a + b;
    this.history.push(result);
    return result;
  }

  getHistory(): number[] {
    return [...this.history];
  }

  clearHistory(): void {
    this.history = [];
  }
}

// math.test.ts
import { add, divide, Calculator } from './math';

describe('Math functions', () => {
  describe('add', () => {
    it('should add two positive numbers', () => {
      expect(add(2, 3)).toBe(5);
    });

    it('should add negative numbers', () => {
      expect(add(-2, -3)).toBe(-5);
    });

    it('should handle zero', () => {
      expect(add(0, 5)).toBe(5);
    });
  });

  describe('divide', () => {
    it('should divide two numbers', () => {
      expect(divide(10, 2)).toBe(5);
    });

    it('should throw error on division by zero', () => {
      expect(() => divide(10, 0)).toThrow('Division by zero');
    });
  });
});

describe('Calculator', () => {
  let calculator: Calculator;

  beforeEach(() => {
    calculator = new Calculator();
  });

  afterEach(() => {
    calculator.clearHistory();
  });

  it('should add numbers and record history', () => {
    calculator.add(2, 3);
    calculator.add(5, 5);

    expect(calculator.getHistory()).toEqual([5, 10]);
  });

  it('should clear history', () => {
    calculator.add(1, 1);
    calculator.clearHistory();

    expect(calculator.getHistory()).toEqual([]);
  });
});
```

### pytest for Python

```python
# calculator.py
class Calculator:
    def __init__(self):
        self.history = []

    def add(self, a, b):
        result = a + b
        self.history.append(result)
        return result

    def divide(self, a, b):
        if b == 0:
            raise ValueError("Division by zero")
        result = a / b
        self.history.append(result)
        return result

    def get_history(self):
        return self.history.copy()

# test_calculator.py
import pytest
from calculator import Calculator

@pytest.fixture
def calculator():
    return Calculator()

class TestCalculator:
    def test_add_positive_numbers(self, calculator):
        result = calculator.add(2, 3)
        assert result == 5

    def test_add_negative_numbers(self, calculator):
        result = calculator.add(-2, -3)
        assert result == -5

    def test_divide(self, calculator):
        result = calculator.divide(10, 2)
        assert result == 5.0

    def test_divide_by_zero_raises_error(self, calculator):
        with pytest.raises(ValueError, match="Division by zero"):
            calculator.divide(10, 0)

    def test_history_tracking(self, calculator):
        calculator.add(2, 3)
        calculator.add(5, 5)
        assert calculator.get_history() == [5, 10]

    @pytest.mark.parametrize("a,b,expected", [
        (1, 1, 2),
        (0, 0, 0),
        (-1, 1, 0),
        (100, 200, 300),
    ])
    def test_add_parametrized(self, calculator, a, b, expected):
        assert calculator.add(a, b) == expected
```

### Testing with Mocks

```javascript
// userService.ts
import axios from 'axios';

export class UserService {
  constructor(private apiUrl: string) {}

  async getUser(id: number) {
    const response = await axios.get(`${this.apiUrl}/users/${id}`);
    return response.data;
  }

  async createUser(userData: any) {
    const response = await axios.post(`${this.apiUrl}/users`, userData);
    return response.data;
  }
}

// userService.test.ts
import axios from 'axios';
import { UserService } from './userService';

jest.mock('axios');
const mockedAxios = axios as jest.Mocked<typeof axios>;

describe('UserService', () => {
  let service: UserService;

  beforeEach(() => {
    service = new UserService('https://api.example.com');
    jest.clearAllMocks();
  });

  describe('getUser', () => {
    it('should fetch user by id', async () => {
      const mockUser = { id: 1, name: 'John Doe' };
      mockedAxios.get.mockResolvedValue({ data: mockUser });

      const user = await service.getUser(1);

      expect(mockedAxios.get).toHaveBeenCalledWith(
        'https://api.example.com/users/1'
      );
      expect(user).toEqual(mockUser);
    });

    it('should handle errors', async () => {
      mockedAxios.get.mockRejectedValue(new Error('Network error'));

      await expect(service.getUser(1)).rejects.toThrow('Network error');
    });
  });

  describe('createUser', () => {
    it('should create a new user', async () => {
      const userData = { name: 'Jane Doe', email: 'jane@example.com' };
      const mockResponse = { id: 2, ...userData };

      mockedAxios.post.mockResolvedValue({ data: mockResponse });

      const result = await service.createUser(userData);

      expect(mockedAxios.post).toHaveBeenCalledWith(
        'https://api.example.com/users',
        userData
      );
      expect(result).toEqual(mockResponse);
    });
  });
});
```

## Integration Testing

### Express API Testing

```javascript
// app.js
const express = require('express');
const app = express();

app.use(express.json());

let users = [];

app.get('/api/users', (req, res) => {
  res.json(users);
});

app.post('/api/users', (req, res) => {
  const user = { id: users.length + 1, ...req.body };
  users.push(user);
  res.status(201).json(user);
});

app.get('/api/users/:id', (req, res) => {
  const user = users.find(u => u.id === parseInt(req.params.id));
  if (!user) {
    return res.status(404).json({ error: 'User not found' });
  }
  res.json(user);
});

module.exports = app;

// app.test.js
const request = require('supertest');
const app = require('./app');

describe('User API', () => {
  beforeEach(() => {
    // Reset users before each test
    users = [];
  });

  describe('GET /api/users', () => {
    it('should return empty array initially', async () => {
      const response = await request(app)
        .get('/api/users')
        .expect(200);

      expect(response.body).toEqual([]);
    });
  });

  describe('POST /api/users', () => {
    it('should create a new user', async () => {
      const userData = {
        name: 'John Doe',
        email: 'john@example.com'
      };

      const response = await request(app)
        .post('/api/users')
        .send(userData)
        .expect(201);

      expect(response.body).toMatchObject(userData);
      expect(response.body.id).toBeDefined();
    });

    it('should validate required fields', async () => {
      const response = await request(app)
        .post('/api/users')
        .send({})
        .expect(400);

      expect(response.body.error).toBeDefined();
    });
  });

  describe('GET /api/users/:id', () => {
    it('should return user by id', async () => {
      // Create a user first
      const createResponse = await request(app)
        .post('/api/users')
        .send({ name: 'Jane Doe', email: 'jane@example.com' });

      const userId = createResponse.body.id;

      // Fetch the user
      const response = await request(app)
        .get(`/api/users/${userId}`)
        .expect(200);

      expect(response.body.id).toBe(userId);
    });

    it('should return 404 for non-existent user', async () => {
      await request(app)
        .get('/api/users/999')
        .expect(404);
    });
  });
});
```

### Database Integration Tests

```python
import pytest
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker
from app.models import Base, User
from app.repositories import UserRepository

@pytest.fixture(scope='function')
def db_session():
    # Create in-memory SQLite database for testing
    engine = create_engine('sqlite:///:memory:')
    Base.metadata.create_all(engine)

    Session = sessionmaker(bind=engine)
    session = Session()

    yield session

    session.close()

@pytest.fixture
def user_repository(db_session):
    return UserRepository(db_session)

class TestUserRepository:
    def test_create_user(self, user_repository):
        user = user_repository.create(
            name='John Doe',
            email='john@example.com'
        )

        assert user.id is not None
        assert user.name == 'John Doe'
        assert user.email == 'john@example.com'

    def test_find_user_by_email(self, user_repository):
        # Create a user
        user_repository.create(
            name='Jane Doe',
            email='jane@example.com'
        )

        # Find by email
        found_user = user_repository.find_by_email('jane@example.com')

        assert found_user is not None
        assert found_user.name == 'Jane Doe'

    def test_update_user(self, user_repository):
        user = user_repository.create(
            name='John Doe',
            email='john@example.com'
        )

        user_repository.update(user.id, name='John Updated')

        updated_user = user_repository.find_by_id(user.id)
        assert updated_user.name == 'John Updated'

    def test_delete_user(self, user_repository):
        user = user_repository.create(
            name='John Doe',
            email='john@example.com'
        )

        user_repository.delete(user.id)

        deleted_user = user_repository.find_by_id(user.id)
        assert deleted_user is None
```

## End-to-End Testing

### Playwright

```typescript
import { test, expect } from '@playwright/test';

test.describe('User Authentication', () => {
  test('should login successfully', async ({ page }) => {
    await page.goto('http://localhost:3000');

    // Fill login form
    await page.fill('[data-testid="email-input"]', 'user@example.com');
    await page.fill('[data-testid="password-input"]', 'password123');
    await page.click('[data-testid="login-button"]');

    // Wait for redirect
    await page.waitForURL('**/dashboard');

    // Verify user is logged in
    await expect(page.locator('[data-testid="user-menu"]')).toBeVisible();
    await expect(page.locator('text=Welcome back')).toBeVisible();
  });

  test('should show error on invalid credentials', async ({ page }) => {
    await page.goto('http://localhost:3000');

    await page.fill('[data-testid="email-input"]', 'wrong@example.com');
    await page.fill('[data-testid="password-input"]', 'wrongpassword');
    await page.click('[data-testid="login-button"]');

    // Verify error message
    await expect(page.locator('[data-testid="error-message"]'))
      .toContainText('Invalid credentials');
  });
});

test.describe('Shopping Cart', () => {
  test.beforeEach(async ({ page }) => {
    // Login before each test
    await page.goto('http://localhost:3000/login');
    await page.fill('[data-testid="email-input"]', 'user@example.com');
    await page.fill('[data-testid="password-input"]', 'password123');
    await page.click('[data-testid="login-button"]');
    await page.waitForURL('**/dashboard');
  });

  test('should add item to cart', async ({ page }) => {
    await page.goto('http://localhost:3000/products');

    // Add first product to cart
    await page.click('[data-testid="add-to-cart"]:first-of-type');

    // Check cart badge
    await expect(page.locator('[data-testid="cart-badge"]'))
      .toContainText('1');

    // Navigate to cart
    await page.click('[data-testid="cart-icon"]');

    // Verify item in cart
    await expect(page.locator('[data-testid="cart-item"]')).toHaveCount(1);
  });

  test('should complete checkout', async ({ page }) => {
    // Add item to cart
    await page.goto('http://localhost:3000/products');
    await page.click('[data-testid="add-to-cart"]:first-of-type');

    // Go to cart
    await page.click('[data-testid="cart-icon"]');

    // Proceed to checkout
    await page.click('[data-testid="checkout-button"]');

    // Fill shipping information
    await page.fill('[name="address"]', '123 Main St');
    await page.fill('[name="city"]', 'New York');
    await page.fill('[name="zipCode"]', '10001');
    await page.click('[data-testid="continue-to-payment"]');

    // Fill payment information
    await page.fill('[name="cardNumber"]', '4242424242424242');
    await page.fill('[name="expiry"]', '12/25');
    await page.fill('[name="cvv"]', '123');

    // Submit order
    await page.click('[data-testid="place-order"]');

    // Verify success
    await expect(page.locator('text=Order confirmed')).toBeVisible();
  });
});
```

### Cypress

```javascript
describe('Todo Application', () => {
  beforeEach(() => {
    cy.visit('http://localhost:3000');
  });

  it('should add a new todo', () => {
    cy.get('[data-cy=todo-input]').type('Buy groceries');
    cy.get('[data-cy=add-button]').click();

    cy.get('[data-cy=todo-list]')
      .should('contain', 'Buy groceries');
  });

  it('should mark todo as complete', () => {
    // Add a todo
    cy.get('[data-cy=todo-input]').type('Walk the dog');
    cy.get('[data-cy=add-button]').click();

    // Mark as complete
    cy.get('[data-cy=todo-item]').first().within(() => {
      cy.get('[data-cy=checkbox]').check();
    });

    // Verify completed state
    cy.get('[data-cy=todo-item]').first()
      .should('have.class', 'completed');
  });

  it('should delete a todo', () => {
    // Add a todo
    cy.get('[data-cy=todo-input]').type('Read a book');
    cy.get('[data-cy=add-button]').click();

    // Delete the todo
    cy.get('[data-cy=todo-item]').first().within(() => {
      cy.get('[data-cy=delete-button]').click();
    });

    // Verify it's removed
    cy.get('[data-cy=todo-list]')
      .should('not.contain', 'Read a book');
  });

  it('should filter todos', () => {
    // Add multiple todos
    ['Todo 1', 'Todo 2', 'Todo 3'].forEach(todo => {
      cy.get('[data-cy=todo-input]').type(todo);
      cy.get('[data-cy=add-button]').click();
    });

    // Mark first todo as complete
    cy.get('[data-cy=todo-item]').first().within(() => {
      cy.get('[data-cy=checkbox]').check();
    });

    // Filter by active
    cy.get('[data-cy=filter-active]').click();
    cy.get('[data-cy=todo-item]').should('have.length', 2);

    // Filter by completed
    cy.get('[data-cy=filter-completed]').click();
    cy.get('[data-cy=todo-item]').should('have.length', 1);
  });
});
```

<Tip>
Use test data builders and factories to create consistent test data across your test suite.
</Tip>

## Test Coverage Calculator

Calculate and visualize test coverage metrics:

<div id="coverage-calculator">
  <style>{`
    .coverage-calc {
      background: linear-gradient(135deg, #2dd4bf 0%, #14b8a6 100%);
      border-radius: 12px;
      padding: 25px;
      margin: 20px 0;
      color: white;
    }
    .coverage-title {
      font-size: 20px;
      font-weight: bold;
      margin-bottom: 20px;
      text-align: center;
    }
    .input-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 15px;
      margin-bottom: 20px;
    }
    .input-field {
      display: flex;
      flex-direction: column;
      gap: 8px;
    }
    .input-label {
      font-size: 12px;
      font-weight: bold;
      text-transform: uppercase;
      opacity: 0.9;
    }
    .coverage-input {
      padding: 12px;
      border: none;
      border-radius: 6px;
      font-size: 16px;
      font-weight: bold;
      text-align: center;
    }
    .coverage-input:focus {
      outline: 3px solid rgba(255, 255, 255, 0.5);
    }
    .metrics-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
      gap: 15px;
      margin: 20px 0;
    }
    .metric-card {
      background: rgba(255, 255, 255, 0.15);
      padding: 20px;
      border-radius: 10px;
      text-align: center;
      backdrop-filter: blur(10px);
      border: 2px solid rgba(255, 255, 255, 0.2);
    }
    .metric-value {
      font-size: 36px;
      font-weight: bold;
      margin: 10px 0;
    }
    .metric-label {
      font-size: 11px;
      text-transform: uppercase;
      opacity: 0.8;
      letter-spacing: 1px;
    }
    .coverage-bar {
      height: 30px;
      background: rgba(0, 0, 0, 0.2);
      border-radius: 15px;
      overflow: hidden;
      margin: 10px 0;
      position: relative;
    }
    .coverage-fill {
      height: 100%;
      background: linear-gradient(90deg, #10b981, #34d399);
      transition: width 0.5s ease;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      font-size: 14px;
    }
    .coverage-excellent { background: linear-gradient(90deg, #10b981, #34d399) !important; }
    .coverage-good { background: linear-gradient(90deg, #f59e0b, #fbbf24) !important; }
    .coverage-poor { background: linear-gradient(90deg, #ef4444, #f87171) !important; }
    .recommendation {
      background: rgba(255, 255, 255, 0.1);
      padding: 15px;
      border-radius: 8px;
      margin-top: 20px;
      font-size: 14px;
      line-height: 1.6;
    }
    .calc-button {
      width: 100%;
      padding: 15px;
      background: white;
      color: #14b8a6;
      border: none;
      border-radius: 8px;
      font-weight: bold;
      font-size: 16px;
      cursor: pointer;
      transition: all 0.3s;
      margin-top: 15px;
    }
    .calc-button:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    }
  `}</style>

  <div class="coverage-calc">
    <div class="coverage-title">Test Coverage Analyzer</div>

    <div class="input-grid">
      <div class="input-field">
        <label class="input-label">Total Lines</label>
        <input type="number" id="total-lines" class="coverage-input" value="1000" min="1" />
      </div>
      <div class="input-field">
        <label class="input-label">Covered Lines</label>
        <input type="number" id="covered-lines" class="coverage-input" value="850" min="0" />
      </div>
      <div class="input-field">
        <label class="input-label">Total Functions</label>
        <input type="number" id="total-functions" class="coverage-input" value="120" min="1" />
      </div>
      <div class="input-field">
        <label class="input-label">Covered Functions</label>
        <input type="number" id="covered-functions" class="coverage-input" value="100" min="0" />
      </div>
      <div class="input-field">
        <label class="input-label">Total Branches</label>
        <input type="number" id="total-branches" class="coverage-input" value="200" min="1" />
      </div>
      <div class="input-field">
        <label class="input-label">Covered Branches</label>
        <input type="number" id="covered-branches" class="coverage-input" value="160" min="0" />
      </div>
    </div>

    <button class="calc-button" onclick="coverageCalc.calculate()">Calculate Coverage</button>

    <div id="coverage-results" style="display: none;">
      <div class="metrics-grid">
        <div class="metric-card">
          <div class="metric-label">Line Coverage</div>
          <div class="metric-value" id="line-coverage">0%</div>
          <div class="coverage-bar">
            <div class="coverage-fill" id="line-bar" style="width: 0%"></div>
          </div>
        </div>
        <div class="metric-card">
          <div class="metric-label">Function Coverage</div>
          <div class="metric-value" id="function-coverage">0%</div>
          <div class="coverage-bar">
            <div class="coverage-fill" id="function-bar" style="width: 0%"></div>
          </div>
        </div>
        <div class="metric-card">
          <div class="metric-label">Branch Coverage</div>
          <div class="metric-value" id="branch-coverage">0%</div>
          <div class="coverage-bar">
            <div class="coverage-fill" id="branch-bar" style="width: 0%"></div>
          </div>
        </div>
        <div class="metric-card">
          <div class="metric-label">Overall Score</div>
          <div class="metric-value" id="overall-score">0%</div>
          <div class="coverage-bar">
            <div class="coverage-fill" id="overall-bar" style="width: 0%"></div>
          </div>
        </div>
      </div>

      <div class="recommendation" id="recommendation"></div>
    </div>
  </div>

  <script dangerouslySetInnerHTML={{__html: `
    const coverageCalc = (function() {
      function getClassName(percentage) {
        if (percentage >= 80) return 'coverage-excellent';
        if (percentage >= 60) return 'coverage-good';
        return 'coverage-poor';
      }

      function getRecommendation(line, func, branch, overall) {
        let recommendations = [];

        if (overall >= 80) {
          recommendations.push('✓ <strong>Excellent coverage!</strong> Your test suite provides comprehensive protection.');
        } else if (overall >= 60) {
          recommendations.push('⚠ <strong>Good coverage</strong>, but there\\'s room for improvement.');
        } else {
          recommendations.push('✗ <strong>Coverage needs improvement.</strong> Consider adding more tests.');
        }

        if (branch < line - 10) {
          recommendations.push('Focus on testing more edge cases and conditional branches.');
        }

        if (func < 90) {
          recommendations.push('Some functions are untested. Aim for at least 90% function coverage.');
        }

        if (line < 70) {
          recommendations.push('Increase line coverage by testing more code paths and error scenarios.');
        }

        const uncoveredLines = parseInt(document.getElementById('total-lines').value) -
                              parseInt(document.getElementById('covered-lines').value);

        recommendations.push(\`<br><strong>Action Items:</strong><br>• \${uncoveredLines} lines need test coverage<br>• Target: 80%+ coverage across all metrics<br>• Review uncovered code for critical paths\`);

        return recommendations.join('<br><br>');
      }

      function calculate() {
        const totalLines = parseInt(document.getElementById('total-lines').value) || 1;
        const coveredLines = parseInt(document.getElementById('covered-lines').value) || 0;
        const totalFunctions = parseInt(document.getElementById('total-functions').value) || 1;
        const coveredFunctions = parseInt(document.getElementById('covered-functions').value) || 0;
        const totalBranches = parseInt(document.getElementById('total-branches').value) || 1;
        const coveredBranches = parseInt(document.getElementById('covered-branches').value) || 0;

        const lineCov = Math.min(100, (coveredLines / totalLines * 100)).toFixed(1);
        const funcCov = Math.min(100, (coveredFunctions / totalFunctions * 100)).toFixed(1);
        const branchCov = Math.min(100, (coveredBranches / totalBranches * 100)).toFixed(1);
        const overallCov = ((parseFloat(lineCov) + parseFloat(funcCov) + parseFloat(branchCov)) / 3).toFixed(1);

        document.getElementById('coverage-results').style.display = 'block';

        // Update line coverage
        document.getElementById('line-coverage').textContent = lineCov + '%';
        const lineBar = document.getElementById('line-bar');
        lineBar.style.width = lineCov + '%';
        lineBar.className = 'coverage-fill ' + getClassName(lineCov);
        lineBar.textContent = lineCov + '%';

        // Update function coverage
        document.getElementById('function-coverage').textContent = funcCov + '%';
        const funcBar = document.getElementById('function-bar');
        funcBar.style.width = funcCov + '%';
        funcBar.className = 'coverage-fill ' + getClassName(funcCov);
        funcBar.textContent = funcCov + '%';

        // Update branch coverage
        document.getElementById('branch-coverage').textContent = branchCov + '%';
        const branchBar = document.getElementById('branch-bar');
        branchBar.style.width = branchCov + '%';
        branchBar.className = 'coverage-fill ' + getClassName(branchCov);
        branchBar.textContent = branchCov + '%';

        // Update overall score
        document.getElementById('overall-score').textContent = overallCov + '%';
        const overallBar = document.getElementById('overall-bar');
        overallBar.style.width = overallCov + '%';
        overallBar.className = 'coverage-fill ' + getClassName(overallCov);
        overallBar.textContent = overallCov + '%';

        // Update recommendation
        document.getElementById('recommendation').innerHTML =
          getRecommendation(parseFloat(lineCov), parseFloat(funcCov), parseFloat(branchCov), parseFloat(overallCov));
      }

      // Auto-calculate on input change
      ['total-lines', 'covered-lines', 'total-functions', 'covered-functions', 'total-branches', 'covered-branches'].forEach(id => {
        const input = document.getElementById(id);
        if (input) {
          input.addEventListener('input', function() {
            if (document.getElementById('coverage-results').style.display !== 'none') {
              calculate();
            }
          });
        }
      });

      return { calculate };
    })();
  `}} />
</div>
