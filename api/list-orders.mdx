---
title: "List Orders"
api: "GET /api/v1/orders"
description: "Retrieve a paginated list of orders with filtering options"
---

## Query Parameters

<ParamField query="page" type="integer" default="1">
  Page number for pagination
</ParamField>

<ParamField query="per_page" type="integer" default="20">
  Number of items per page (max: 100)
</ParamField>

<ParamField query="status" type="string">
  Filter by order status: `pending`, `processing`, `shipped`, `delivered`, `cancelled`
</ParamField>

<ParamField query="user_id" type="string">
  Filter orders by user ID
</ParamField>

<ParamField query="from_date" type="string">
  Filter orders created after this date (ISO 8601 format)
</ParamField>

<ParamField query="to_date" type="string">
  Filter orders created before this date (ISO 8601 format)
</ParamField>

<ParamField query="min_total" type="number">
  Minimum order total amount
</ParamField>

<ParamField query="max_total" type="number">
  Maximum order total amount
</ParamField>

<ParamField query="sort_by" type="string" default="created_at">
  Sort field: `created_at`, `total_amount`, `status`
</ParamField>

<ParamField query="order" type="string" default="desc">
  Sort order: `asc` or `desc`
</ParamField>

## Response

<ResponseField name="data" type="array">
  Array of order objects
  <Expandable title="order object">
    <ResponseField name="id" type="string">
      Order unique identifier
    </ResponseField>
    <ResponseField name="user_id" type="string">
      User who placed the order
    </ResponseField>
    <ResponseField name="items" type="array">
      Array of order items
    </ResponseField>
    <ResponseField name="total_amount" type="number">
      Total order amount
    </ResponseField>
    <ResponseField name="currency" type="string">
      Currency code
    </ResponseField>
    <ResponseField name="status" type="string">
      Order status
    </ResponseField>
    <ResponseField name="shipping_address" type="object">
      Shipping address details
    </ResponseField>
    <ResponseField name="created_at" type="string">
      Order creation timestamp
    </ResponseField>
  </Expandable>
</ResponseField>

<RequestExample>
```bash cURL
curl -X GET "https://api.example.com/api/v1/orders?status=processing&page=1" \
  -H "Authorization: Bearer YOUR_API_KEY"
```

```python Python
import requests

url = "https://api.example.com/api/v1/orders"
headers = {"Authorization": "Bearer YOUR_API_KEY"}
params = {"status": "processing", "page": 1}

response = requests.get(url, headers=headers, params=params)
print(response.json())
```

```javascript JavaScript
const response = await fetch('https://api.example.com/api/v1/orders?status=processing', {
  headers: { 'Authorization': 'Bearer YOUR_API_KEY' }
});
const data = await response.json();
```
</RequestExample>

<ResponseExample>
```json 200 Success
{
  "data": [
    {
      "id": "order_123",
      "user_id": "user_456",
      "items": [
        {
          "product_id": "prod_789",
          "quantity": 2,
          "price": 49.99
        }
      ],
      "total_amount": 99.98,
      "currency": "USD",
      "status": "processing",
      "shipping_address": {
        "street": "123 Main St",
        "city": "New York",
        "state": "NY",
        "zip": "10001",
        "country": "US"
      },
      "created_at": "2024-01-15T10:30:00Z"
    }
  ],
  "pagination": {
    "current_page": 1,
    "per_page": 20,
    "total_pages": 10,
    "total_count": 195
  }
}
```
</ResponseExample>
